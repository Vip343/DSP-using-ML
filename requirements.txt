# Audio Processing
librosa>=0.10.1
soundfile>=0.12.1
audioread>=3.0.1

# Signal Processing / DSP
scipy>=1.11.0
numpy>=1.24.0

# AI Models for Denoising
torch>=2.0.0
torchaudio>=2.0.0
demucs>=4.0.0

# Evaluation Metrics
pystoi>=0.4.1
mir_eval>=0.7

# Alternative AI Denoiser (no Rust required)
noisereduce>=3.0.0

# SpeechBrain - Speech enhancement with MetricGAN+
speechbrain>=1.0.0

# Note: RNNoise is not available as Python library for macOS
# Use noisereduce as alternative (spectral gating)

# Hugging Face API for DeepFilterNet2
gradio_client>=0.8.0

# Data Handling
pandas>=2.0.0
packaging>=21.0  # For version comparison

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars
tqdm>=4.65.0

# ============================================================
# OPTIONAL: Requires Rust compiler (see README for instructions)
# Install Rust first: curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
# Then run: pip install deepfilternet pesq
# ============================================================
